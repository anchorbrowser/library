{
  "slug": "export-reconciliation-dashboards",
  "name": "Export Reconciliation Dashboards",
  "description": "Export reconciliation status dashboards from BlackLine.",
  "type": "tool",
  "file": "export-reconciliation-dashboards.ts",
  "app": "blackline",
  "inputSchema": [
    {
      "display_name": "closePeriod",
      "type": "date",
      "required": true,
      "description": "Close period date"
    },
    {
      "display_name": "dashboardId",
      "type": "string",
      "required": false,
      "description": "Specific dashboard ID"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to exported dashboard"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether export completed"
    }
  ],
  "tags": [
    "finance",
    "reconciliation",
    "export"
  ]
}
