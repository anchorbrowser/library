{
  "slug": "export-policy-configs",
  "name": "Export Policy Configs",
  "description": "Export policy configurations from Zscaler Admin.",
  "type": "tool",
  "file": "export-policy-configs.ts",
  "app": "zscaler-admin",
  "inputSchema": [
    {
      "display_name": "policyType",
      "type": "string",
      "required": false,
      "description": "Policy type filter"
    },
    {
      "display_name": "format",
      "type": "string",
      "required": false,
      "description": "Export format (JSON, CSV)"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to exported configs"
    },
    {
      "display_name": "policyCount",
      "type": "number",
      "required": true,
      "description": "Number of policies exported"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether export completed"
    }
  ],
  "tags": [
    "security",
    "network",
    "export"
  ]
}
