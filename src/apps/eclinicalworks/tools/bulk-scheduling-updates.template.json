{
  "slug": "bulk-scheduling-updates",
  "name": "Bulk Scheduling Updates",
  "description": "Bulk scheduling updates from external calendars in eClinicalWorks.",
  "type": "tool",
  "file": "bulk-scheduling-updates.ts",
  "app": "eclinicalworks",
  "inputSchema": [
    {
      "display_name": "calendarSource",
      "type": "string",
      "required": true,
      "description": "External calendar source URL or ID"
    },
    {
      "display_name": "dateFrom",
      "type": "date",
      "required": true,
      "description": "Start date for scheduling window"
    },
    {
      "display_name": "dateTo",
      "type": "date",
      "required": true,
      "description": "End date for scheduling window"
    }
  ],
  "outputSchema": [
    {
      "display_name": "updatedCount",
      "type": "number",
      "required": true,
      "description": "Number of appointments updated"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether updates completed"
    }
  ],
  "tags": [
    "healthcare",
    "ehr",
    "automation"
  ]
}
