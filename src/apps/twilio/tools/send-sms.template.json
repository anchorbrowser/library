{
  "slug": "send-sms",
  "name": "Send SMS",
  "description": "Send an SMS message via Twilio.",
  "type": "tool",
  "file": "send-sms.ts",
  "app": "twilio",
  "inputSchema": [
    { "display_name": "to", "type": "string", "required": true, "description": "Recipient phone number" },
    { "display_name": "from", "type": "string", "required": true, "description": "Twilio phone number" },
    { "display_name": "body", "type": "string", "required": true, "description": "Message body" }
  ],
  "outputSchema": [
    { "display_name": "messageSid", "type": "string", "required": true, "description": "Message SID" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether SMS was sent" }
  ]
}
