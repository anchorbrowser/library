{
  "slug": "get-incidents",
  "name": "Get Incidents",
  "description": "Get incidents from PagerDuty.",
  "type": "tool",
  "file": "get-incidents.ts",
  "app": "pagerduty",
  "inputSchema": [
    { "displayName": "status", "type": "string", "required": false, "description": "Filter by status (triggered, acknowledged, resolved)", "defaultValue": "triggered" }
  ],
  "outputSchema": [
    { "displayName": "incidents", "type": "string", "required": true, "description": "JSON array of incidents" },
    { "displayName": "totalCount", "type": "number", "required": true, "description": "Total count" }
  ]
}
