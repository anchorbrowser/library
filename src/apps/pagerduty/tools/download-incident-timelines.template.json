{
  "slug": "download-incident-timelines",
  "name": "Download Incident Timelines",
  "description": "Download incident timelines from PagerDuty.",
  "type": "tool",
  "file": "download-incident-timelines.ts",
  "app": "pagerduty",
  "inputSchema": [
    {
      "display_name": "incidentId",
      "type": "string",
      "required": false,
      "description": "Specific incident ID"
    },
    {
      "display_name": "dateFrom",
      "type": "date",
      "required": true,
      "description": "Start date"
    },
    {
      "display_name": "dateTo",
      "type": "date",
      "required": true,
      "description": "End date"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to downloaded timelines"
    },
    {
      "display_name": "incidentCount",
      "type": "number",
      "required": true,
      "description": "Number of incidents"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether download completed"
    }
  ]
}
