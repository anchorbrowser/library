{
  "slug": "export-billing-breakdown",
  "name": "Export Billing Cost Breakdown",
  "description": "Export billing cost breakdown from AWS Console.",
  "type": "tool",
  "file": "export-billing-breakdown.ts",
  "app": "aws-console",
  "inputSchema": [
    {
      "display_name": "month",
      "type": "string",
      "required": true,
      "description": "Billing month (YYYY-MM)"
    },
    {
      "display_name": "groupBy",
      "type": "string",
      "required": false,
      "description": "Group by (service, region)"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to cost breakdown"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether export completed"
    }
  ]
}
