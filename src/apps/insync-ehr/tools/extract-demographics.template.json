{
  "slug": "extract-demographics",
  "name": "Extract Demographics",
  "description": "Extract demographics for population analysis from InSync EHR.",
  "type": "tool",
  "file": "extract-demographics.ts",
  "app": "insync-ehr",
  "inputSchema": [
    {
      "display_name": "populationFilter",
      "type": "string",
      "required": false,
      "description": "Population segment filter"
    },
    {
      "display_name": "format",
      "type": "string",
      "required": false,
      "description": "Export format (CSV, JSON)"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to extracted data"
    },
    {
      "display_name": "patientCount",
      "type": "number",
      "required": true,
      "description": "Number of patient records"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether extraction completed"
    }
  ]
}
