{
  "slug": "send-envelope",
  "name": "Send Envelope",
  "description": "Send a document for signature via DocuSign.",
  "type": "tool",
  "file": "send-envelope.ts",
  "app": "docusign",
  "inputSchema": [
    { "displayName": "recipientEmail", "type": "string", "required": true, "description": "Recipient email" },
    { "displayName": "recipientName", "type": "string", "required": true, "description": "Recipient name" },
    { "displayName": "subject", "type": "string", "required": true, "description": "Email subject" },
    { "displayName": "message", "type": "string", "required": false, "description": "Email message" }
  ],
  "outputSchema": [
    { "displayName": "envelopeId", "type": "string", "required": true, "description": "Envelope ID" },
    { "displayName": "status", "type": "string", "required": true, "description": "Envelope status" },
    { "displayName": "success", "type": "boolean", "required": true, "description": "Whether envelope was sent" }
  ]
}
