{
  "slug": "send-envelope",
  "name": "Send Envelope",
  "description": "Send a document for signature in DocuSign.",
  "type": "tool",
  "file": "send-envelope.ts",
  "app": "docusign",
  "inputSchema": [
    { "display_name": "recipientEmail", "type": "string", "required": true, "description": "Recipient email" },
    { "display_name": "recipientName", "type": "string", "required": true, "description": "Recipient name" },
    { "display_name": "subject", "type": "string", "required": true, "description": "Email subject" },
    { "display_name": "templateId", "type": "string", "required": false, "description": "Template ID to use" }
  ],
  "outputSchema": [
    { "display_name": "envelopeId", "type": "string", "required": true, "description": "Created envelope ID" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether envelope was sent" }
  ]
}
