{
  "slug": "search-accounts",
  "name": "Search Accounts",
  "description": "Search for accounts in Salesforce by name or other criteria.",
  "type": "tool",
  "file": "search-accounts.ts",
  "app": "salesforce",
  "inputSchema": [
    { "display_name": "searchQuery", "type": "string", "required": true, "description": "Search term for account name" },
    { "display_name": "limit", "type": "number", "required": false, "description": "Maximum results to return", "defaultValue": "10" }
  ],
  "outputSchema": [
    { "display_name": "accounts", "type": "string", "required": true, "description": "JSON array of matching accounts" },
    { "display_name": "totalCount", "type": "number", "required": true, "description": "Total number of matches" }
  ]
}
