{
  "slug": "search-accounts",
  "name": "Search Accounts",
  "description": "Search for accounts in Salesforce by name or other criteria.",
  "type": "tool",
  "file": "search-accounts.ts",
  "app": "salesforce",
  "inputSchema": [
    { "displayName": "searchQuery", "type": "string", "required": true, "description": "Search term for account name" },
    { "displayName": "limit", "type": "number", "required": false, "description": "Maximum results to return", "defaultValue": "10" }
  ],
  "outputSchema": [
    { "displayName": "accounts", "type": "string", "required": true, "description": "JSON array of matching accounts" },
    { "displayName": "totalCount", "type": "number", "required": true, "description": "Total number of matches" }
  ]
}
