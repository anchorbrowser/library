{
  "slug": "create-lead",
  "name": "Create Lead",
  "description": "Create a new lead in Salesforce with contact and company information.",
  "type": "tool",
  "file": "create-lead.ts",
  "app": "salesforce",
  "inputSchema": [
    { "display_name": "firstName", "type": "string", "required": true, "description": "Lead's first name" },
    { "display_name": "lastName", "type": "string", "required": true, "description": "Lead's last name" },
    { "display_name": "email", "type": "string", "required": true, "description": "Lead's email address" },
    { "display_name": "company", "type": "string", "required": true, "description": "Company name" },
    { "display_name": "phone", "type": "string", "required": false, "description": "Phone number" },
    { "display_name": "title", "type": "string", "required": false, "description": "Job title" }
  ],
  "outputSchema": [
    { "display_name": "leadId", "type": "string", "required": true, "description": "Created lead ID" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether lead was created" }
  ]
}
