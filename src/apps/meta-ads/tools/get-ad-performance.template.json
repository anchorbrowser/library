{
  "slug": "get-ad-performance",
  "name": "Get Ad Performance",
  "description": "Get performance metrics for Meta/Facebook ads.",
  "type": "tool",
  "file": "get-ad-performance.ts",
  "app": "meta-ads",
  "inputSchema": [
    { "displayName": "dateRange", "type": "string", "required": false, "description": "Date range", "defaultValue": "Last 7 days" }
  ],
  "outputSchema": [
    { "displayName": "campaigns", "type": "string", "required": true, "description": "JSON array of campaign metrics" },
    { "displayName": "totalSpend", "type": "number", "required": true, "description": "Total spend" },
    { "displayName": "totalReach", "type": "number", "required": true, "description": "Total reach" }
  ]
}
