{
  "slug": "export-transactions",
  "name": "Export Transactions",
  "description": "Export transactions from Brex for a date range.",
  "type": "tool",
  "file": "export-transactions.ts",
  "app": "brex",
  "inputSchema": [
    { "display_name": "startDate", "type": "date", "required": true, "description": "Start date" },
    { "display_name": "endDate", "type": "date", "required": true, "description": "End date" },
    { "display_name": "cardId", "type": "string", "required": false, "description": "Specific card ID (optional)" }
  ],
  "outputSchema": [
    { "display_name": "transactionCount", "type": "number", "required": true, "description": "Number of transactions" },
    { "display_name": "totalSpend", "type": "number", "required": true, "description": "Total spend amount" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether export succeeded" }
  ]
}
