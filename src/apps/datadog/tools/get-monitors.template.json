{
  "slug": "get-monitors",
  "name": "Get Monitors",
  "description": "Get status of monitors in Datadog.",
  "type": "tool",
  "file": "get-monitors.ts",
  "app": "datadog",
  "inputSchema": [
    { "displayName": "status", "type": "string", "required": false, "description": "Filter by status (OK, Alert, Warn)", "defaultValue": "All" }
  ],
  "outputSchema": [
    { "displayName": "monitors", "type": "string", "required": true, "description": "JSON array of monitors" },
    { "displayName": "alertCount", "type": "number", "required": true, "description": "Number of alerting monitors" }
  ]
}
