{
  "slug": "get-metrics",
  "name": "Get Metrics",
  "description": "Get metrics data from Datadog.",
  "type": "tool",
  "file": "get-metrics.ts",
  "app": "datadog",
  "inputSchema": [
    { "display_name": "query", "type": "string", "required": true, "description": "Metrics query" },
    { "display_name": "timeRange", "type": "string", "required": false, "description": "Time range" }
  ],
  "outputSchema": [
    { "display_name": "metrics", "type": "string", "required": true, "description": "JSON metrics data" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether metrics were retrieved" }
  ]
}
