{
  "slug": "monitor-ticket-availability",
  "name": "Monitor Ticket Availability",
  "description": "Monitor ticket availability on Ticketmaster.",
  "type": "tool",
  "file": "monitor-ticket-availability.ts",
  "app": "ticketmaster",
  "inputSchema": [
    {
      "display_name": "eventId",
      "type": "string",
      "required": true,
      "description": "Event ID"
    },
    {
      "display_name": "targetPrice",
      "type": "string",
      "required": false,
      "description": "Maximum price filter"
    }
  ],
  "outputSchema": [
    {
      "display_name": "available",
      "type": "boolean",
      "required": true,
      "description": "Whether tickets available"
    },
    {
      "display_name": "lowestPrice",
      "type": "string",
      "required": true,
      "description": "Lowest price found"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether monitor completed"
    }
  ]
}
