{
  "slug": "download-invoices",
  "name": "Download Invoices",
  "description": "Download invoices from supplier portals.",
  "type": "tool",
  "file": "download-invoices.ts",
  "app": "supplier-portals",
  "inputSchema": [
    {
      "display_name": "vendorId",
      "type": "string",
      "required": false,
      "description": "Vendor identifier"
    },
    {
      "display_name": "dateFrom",
      "type": "date",
      "required": true,
      "description": "Start date"
    },
    {
      "display_name": "dateTo",
      "type": "date",
      "required": true,
      "description": "End date"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to downloaded invoices"
    },
    {
      "display_name": "invoiceCount",
      "type": "number",
      "required": true,
      "description": "Number of invoices downloaded"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether download completed"
    }
  ],
  "tags": [
    "procurement",
    "invoicing",
    "export"
  ]
}
