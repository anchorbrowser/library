{
  "slug": "create-deal",
  "name": "Create Deal",
  "description": "Create a new deal in HubSpot sales pipeline.",
  "type": "tool",
  "file": "create-deal.ts",
  "app": "hubspot",
  "inputSchema": [
    { "displayName": "dealName", "type": "string", "required": true, "description": "Name of the deal" },
    { "displayName": "amount", "type": "number", "required": true, "description": "Deal amount" },
    { "displayName": "stage", "type": "string", "required": false, "description": "Pipeline stage", "defaultValue": "Qualification" },
    { "displayName": "contactEmail", "type": "string", "required": false, "description": "Associated contact email" }
  ],
  "outputSchema": [
    { "displayName": "dealId", "type": "string", "required": true, "description": "Created deal ID" },
    { "displayName": "success", "type": "boolean", "required": true, "description": "Whether deal was created" }
  ]
}
