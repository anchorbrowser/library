{
  "slug": "create-deal",
  "name": "Create Deal",
  "description": "Create a new deal in HubSpot sales pipeline.",
  "type": "tool",
  "file": "create-deal.ts",
  "app": "hubspot",
  "inputSchema": [
    { "display_name": "dealName", "type": "string", "required": true, "description": "Name of the deal" },
    { "display_name": "amount", "type": "number", "required": true, "description": "Deal amount" },
    { "display_name": "stage", "type": "string", "required": false, "description": "Pipeline stage", "defaultValue": "Qualification" },
    { "display_name": "contactEmail", "type": "string", "required": false, "description": "Associated contact email" }
  ],
  "outputSchema": [
    { "display_name": "dealId", "type": "string", "required": true, "description": "Created deal ID" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether deal was created" }
  ]
}
