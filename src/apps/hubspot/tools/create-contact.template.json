{
  "slug": "create-contact",
  "name": "Create Contact",
  "description": "Create a new contact in HubSpot CRM.",
  "type": "tool",
  "file": "create-contact.ts",
  "app": "hubspot",
  "inputSchema": [
    { "displayName": "email", "type": "string", "required": true, "description": "Contact email address" },
    { "displayName": "firstName", "type": "string", "required": true, "description": "First name" },
    { "displayName": "lastName", "type": "string", "required": true, "description": "Last name" },
    { "displayName": "phone", "type": "string", "required": false, "description": "Phone number" },
    { "displayName": "company", "type": "string", "required": false, "description": "Company name" }
  ],
  "outputSchema": [
    { "displayName": "contactId", "type": "string", "required": true, "description": "Created contact ID" },
    { "displayName": "success", "type": "boolean", "required": true, "description": "Whether contact was created" }
  ]
}
