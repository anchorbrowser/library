{
  "slug": "get-chart",
  "name": "Get Chart",
  "description": "Get analytics chart data from Amplitude.",
  "type": "tool",
  "file": "get-chart.ts",
  "app": "amplitude",
  "inputSchema": [
    { "display_name": "chartId", "type": "string", "required": false, "description": "Chart ID (or new)" },
    { "display_name": "event", "type": "string", "required": true, "description": "Event to analyze" },
    { "display_name": "dateRange", "type": "string", "required": false, "description": "Date range" }
  ],
  "outputSchema": [
    { "display_name": "chartData", "type": "string", "required": true, "description": "JSON chart data" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether data was retrieved" }
  ]
}
