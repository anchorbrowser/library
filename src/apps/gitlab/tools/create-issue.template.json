{
  "slug": "create-issue",
  "name": "Create Issue",
  "description": "Create a new issue in a GitLab project.",
  "type": "tool",
  "file": "create-issue.ts",
  "app": "gitlab",
  "inputSchema": [
    { "displayName": "project", "type": "string", "required": true, "description": "Project path (group/project)" },
    { "displayName": "title", "type": "string", "required": true, "description": "Issue title" },
    { "displayName": "description", "type": "string", "required": false, "description": "Issue description" }
  ],
  "outputSchema": [
    { "displayName": "issueId", "type": "number", "required": true, "description": "Issue ID" },
    { "displayName": "issueUrl", "type": "string", "required": true, "description": "Issue URL" },
    { "displayName": "success", "type": "boolean", "required": true, "description": "Whether issue was created" }
  ]
}
