{
  "slug": "send-message",
  "name": "Send Message",
  "description": "Send a message to a user or conversation in Intercom.",
  "type": "tool",
  "file": "send-message.ts",
  "app": "intercom",
  "inputSchema": [
    { "display_name": "conversationId", "type": "string", "required": true, "description": "Conversation ID or user email" },
    { "display_name": "message", "type": "string", "required": true, "description": "Message content" }
  ],
  "outputSchema": [
    { "display_name": "messageId", "type": "string", "required": true, "description": "Sent message ID" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether message was sent" }
  ]
}
