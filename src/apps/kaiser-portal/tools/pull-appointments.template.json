{
  "slug": "pull-appointments",
  "name": "Pull Upcoming Appointment List",
  "description": "Pull upcoming appointment list from Kaiser Portal.",
  "type": "tool",
  "file": "pull-appointments.ts",
  "app": "kaiser-portal",
  "inputSchema": [
    {
      "display_name": "dateFrom",
      "type": "date",
      "required": false,
      "description": "Start date filter"
    },
    {
      "display_name": "dateTo",
      "type": "date",
      "required": false,
      "description": "End date filter"
    }
  ],
  "outputSchema": [
    {
      "display_name": "appointments",
      "type": "string",
      "required": true,
      "description": "JSON array of appointments"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether fetch completed"
    }
  ]
}
