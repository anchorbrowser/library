{
  "slug": "get-source-status",
  "name": "Get Source Status",
  "description": "Get status of data sources in Segment.",
  "type": "tool",
  "file": "get-source-status.ts",
  "app": "segment",
  "inputSchema": [
    { "displayName": "workspaceSlug", "type": "string", "required": true, "description": "Workspace slug" }
  ],
  "outputSchema": [
    { "displayName": "sources", "type": "string", "required": true, "description": "JSON array of sources with status" },
    { "displayName": "activeCount", "type": "number", "required": true, "description": "Number of active sources" }
  ]
}
