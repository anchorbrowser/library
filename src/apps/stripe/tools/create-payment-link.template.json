{
  "slug": "create-payment-link",
  "name": "Create Payment Link",
  "description": "Create a new payment link in Stripe.",
  "type": "tool",
  "file": "create-payment-link.ts",
  "app": "stripe",
  "inputSchema": [
    { "display_name": "productName", "type": "string", "required": true, "description": "Product or service name" },
    { "display_name": "amount", "type": "number", "required": true, "description": "Amount in cents" },
    { "display_name": "currency", "type": "string", "required": false, "description": "Currency code", "defaultValue": "usd" }
  ],
  "outputSchema": [
    { "display_name": "paymentLinkUrl", "type": "string", "required": true, "description": "Payment link URL" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether link was created" }
  ]
}
