{
  "slug": "create-payment-link",
  "name": "Create Payment Link",
  "description": "Create a new payment link in Stripe.",
  "type": "tool",
  "file": "create-payment-link.ts",
  "app": "stripe",
  "inputSchema": [
    { "displayName": "productName", "type": "string", "required": true, "description": "Product or service name" },
    { "displayName": "amount", "type": "number", "required": true, "description": "Amount in cents" },
    { "displayName": "currency", "type": "string", "required": false, "description": "Currency code", "defaultValue": "usd" }
  ],
  "outputSchema": [
    { "displayName": "paymentLinkUrl", "type": "string", "required": true, "description": "Payment link URL" },
    { "displayName": "success", "type": "boolean", "required": true, "description": "Whether link was created" }
  ]
}
