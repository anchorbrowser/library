{
  "slug": "get-report",
  "name": "Get Report",
  "description": "Get analytics report from Google Analytics.",
  "type": "tool",
  "file": "get-report.ts",
  "app": "google-analytics",
  "inputSchema": [
    { "display_name": "propertyId", "type": "string", "required": true, "description": "GA4 property ID" },
    { "display_name": "dateRange", "type": "string", "required": false, "description": "Date range" },
    { "display_name": "metrics", "type": "string", "required": false, "description": "Metrics to include" }
  ],
  "outputSchema": [
    { "display_name": "report", "type": "string", "required": true, "description": "JSON report data" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether report was retrieved" }
  ]
}
