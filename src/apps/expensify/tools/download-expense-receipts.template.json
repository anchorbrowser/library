{
  "slug": "download-expense-receipts",
  "name": "Download Expense Receipts",
  "description": "Download expense receipts archive from Expensify.",
  "type": "tool",
  "file": "download-expense-receipts.ts",
  "app": "expensify",
  "inputSchema": [
    {
      "display_name": "dateFrom",
      "type": "date",
      "required": true,
      "description": "Start date filter"
    },
    {
      "display_name": "dateTo",
      "type": "date",
      "required": true,
      "description": "End date filter"
    },
    {
      "display_name": "employeeEmail",
      "type": "string",
      "required": false,
      "description": "Filter by employee email"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to downloaded receipts"
    },
    {
      "display_name": "receiptCount",
      "type": "number",
      "required": true,
      "description": "Number of receipts downloaded"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether download completed"
    }
  ]
}
