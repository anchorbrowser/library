{
  "slug": "get-stats",
  "name": "Get Stats",
  "description": "Get email statistics from SendGrid.",
  "type": "tool",
  "file": "get-stats.ts",
  "app": "sendgrid",
  "inputSchema": [
    { "display_name": "startDate", "type": "string", "required": true, "description": "Start date (YYYY-MM-DD)" },
    { "display_name": "endDate", "type": "string", "required": false, "description": "End date (YYYY-MM-DD)" }
  ],
  "outputSchema": [
    { "display_name": "stats", "type": "string", "required": true, "description": "JSON email stats" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether stats were retrieved" }
  ]
}
