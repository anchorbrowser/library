{
  "slug": "get-deploys",
  "name": "Get Deploys",
  "description": "Get deploys for a Netlify site.",
  "type": "tool",
  "file": "get-deploys.ts",
  "app": "netlify",
  "inputSchema": [
    { "display_name": "siteName", "type": "string", "required": true, "description": "Site name" },
    { "display_name": "limit", "type": "number", "required": false, "description": "Max deploys to return" }
  ],
  "outputSchema": [
    { "display_name": "deploys", "type": "string", "required": true, "description": "JSON array of deploys" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether deploys were retrieved" }
  ]
}
