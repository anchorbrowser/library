{
  "slug": "get-campaign-performance",
  "name": "Get Campaign Performance",
  "description": "Get performance metrics for Google Ads campaigns.",
  "type": "tool",
  "file": "get-campaign-performance.ts",
  "app": "google-ads",
  "inputSchema": [
    { "displayName": "dateRange", "type": "string", "required": false, "description": "Date range (Last 7 days, Last 30 days, etc.)", "defaultValue": "Last 7 days" }
  ],
  "outputSchema": [
    { "displayName": "campaigns", "type": "string", "required": true, "description": "JSON array of campaign metrics" },
    { "displayName": "totalSpend", "type": "number", "required": true, "description": "Total spend" },
    { "displayName": "totalClicks", "type": "number", "required": true, "description": "Total clicks" }
  ]
}
