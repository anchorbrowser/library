{
  "slug": "monitor-fare-drop",
  "name": "Monitor Fare Price Drop",
  "description": "Monitor fare price drop on Southwest Airlines.",
  "type": "tool",
  "file": "monitor-fare-drop.ts",
  "app": "southwest-airlines",
  "inputSchema": [
    {
      "display_name": "origin",
      "type": "string",
      "required": true,
      "description": "Origin airport code"
    },
    {
      "display_name": "destination",
      "type": "string",
      "required": true,
      "description": "Destination airport code"
    },
    {
      "display_name": "travelDate",
      "type": "date",
      "required": true,
      "description": "Travel date"
    },
    {
      "display_name": "targetPrice",
      "type": "string",
      "required": true,
      "description": "Target price threshold"
    }
  ],
  "outputSchema": [
    {
      "display_name": "currentPrice",
      "type": "string",
      "required": true,
      "description": "Current lowest fare"
    },
    {
      "display_name": "priceDrop",
      "type": "boolean",
      "required": true,
      "description": "Whether price dropped below target"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether monitor completed"
    }
  ],
  "tags": [
    "travel",
    "airlines",
    "monitoring"
  ]
}
