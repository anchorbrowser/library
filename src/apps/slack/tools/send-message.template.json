{
  "slug": "send-message",
  "name": "Send Message",
  "description": "Send a message to a Slack channel or user.",
  "type": "tool",
  "file": "send-message.ts",
  "app": "slack",
  "inputSchema": [
    { "displayName": "channel", "type": "string", "required": true, "description": "Channel name or user name" },
    { "displayName": "message", "type": "string", "required": true, "description": "Message content" }
  ],
  "outputSchema": [
    { "displayName": "messageId", "type": "string", "required": true, "description": "Sent message ID" },
    { "displayName": "success", "type": "boolean", "required": true, "description": "Whether message was sent" }
  ]
}
