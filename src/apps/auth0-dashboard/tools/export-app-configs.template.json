{
  "slug": "export-app-configs",
  "name": "Export App Configs",
  "description": "Bulk application configuration exports from Auth0 Dashboard.",
  "type": "tool",
  "file": "export-app-configs.ts",
  "app": "auth0-dashboard",
  "inputSchema": [
    {
      "display_name": "appId",
      "type": "string",
      "required": false,
      "description": "Specific application ID"
    },
    {
      "display_name": "environment",
      "type": "string",
      "required": false,
      "description": "Environment filter (dev, staging, prod)"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to exported configs"
    },
    {
      "display_name": "appCount",
      "type": "number",
      "required": true,
      "description": "Number of apps exported"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether export completed"
    }
  ]
}
