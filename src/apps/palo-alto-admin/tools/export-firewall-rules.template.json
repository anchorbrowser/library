{
  "slug": "export-firewall-rules",
  "name": "Export Firewall Rules",
  "description": "Export firewall rule sets from Palo Alto Admin.",
  "type": "tool",
  "file": "export-firewall-rules.ts",
  "app": "palo-alto-admin",
  "inputSchema": [
    {
      "display_name": "deviceGroup",
      "type": "string",
      "required": false,
      "description": "Device group filter"
    },
    {
      "display_name": "ruleType",
      "type": "string",
      "required": false,
      "description": "Rule type filter (security, NAT, etc.)"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to exported rules"
    },
    {
      "display_name": "ruleCount",
      "type": "number",
      "required": true,
      "description": "Number of rules exported"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether export completed"
    }
  ],
  "tags": [
    "security",
    "firewall",
    "export"
  ]
}
