{
  "slug": "get-orders",
  "name": "Get Orders",
  "description": "Get recent orders from Amazon Seller Central.",
  "type": "tool",
  "file": "get-orders.ts",
  "app": "amazon-seller",
  "inputSchema": [
    { "displayName": "status", "type": "string", "required": false, "description": "Order status filter (Pending, Unshipped, Shipped)", "defaultValue": "All" },
    { "displayName": "limit", "type": "number", "required": false, "description": "Maximum orders to return", "defaultValue": "10" }
  ],
  "outputSchema": [
    { "displayName": "orders", "type": "string", "required": true, "description": "JSON array of orders" },
    { "displayName": "totalCount", "type": "number", "required": true, "description": "Total order count" }
  ]
}
