{
  "slug": "create-invoice",
  "name": "Create Invoice",
  "description": "Create a new invoice in QuickBooks.",
  "type": "tool",
  "file": "create-invoice.ts",
  "app": "quickbooks",
  "inputSchema": [
    { "display_name": "customerName", "type": "string", "required": true, "description": "Customer name" },
    { "display_name": "items", "type": "string", "required": true, "description": "JSON array of line items [{description, quantity, rate}]" },
    { "display_name": "dueDate", "type": "date", "required": false, "description": "Invoice due date" },
    { "display_name": "memo", "type": "string", "required": false, "description": "Invoice memo/notes" }
  ],
  "outputSchema": [
    { "display_name": "invoiceNumber", "type": "string", "required": true, "description": "Created invoice number" },
    { "display_name": "totalAmount", "type": "number", "required": true, "description": "Invoice total" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether invoice was created" }
  ]
}
