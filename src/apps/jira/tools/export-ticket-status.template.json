{
  "slug": "export-ticket-status",
  "name": "Export Ticket Status",
  "description": "Bulk ticket status exports from Jira.",
  "type": "tool",
  "file": "export-ticket-status.ts",
  "app": "jira",
  "inputSchema": [
    {
      "display_name": "projectKey",
      "type": "string",
      "required": true,
      "description": "Jira project key"
    },
    {
      "display_name": "statusFilter",
      "type": "string",
      "required": false,
      "description": "Filter by ticket status"
    },
    {
      "display_name": "dateFrom",
      "type": "date",
      "required": false,
      "description": "Start date filter"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to exported data"
    },
    {
      "display_name": "ticketCount",
      "type": "number",
      "required": true,
      "description": "Number of tickets exported"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether export completed"
    }
  ],
  "tags": [
    "developer",
    "project-management",
    "export"
  ]
}
