{
  "slug": "bulk-update-statuses",
  "name": "Bulk Update Ticket Statuses",
  "description": "Bulk update ticket statuses in Jira.",
  "type": "tool",
  "file": "bulk-update-statuses.ts",
  "app": "jira",
  "inputSchema": [
    {
      "display_name": "issueKeys",
      "type": "string",
      "required": true,
      "description": "Comma-separated issue keys"
    },
    {
      "display_name": "targetStatus",
      "type": "string",
      "required": true,
      "description": "Target status"
    }
  ],
  "outputSchema": [
    {
      "display_name": "updatedCount",
      "type": "string",
      "required": true,
      "description": "Number of issues updated"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether update completed"
    }
  ],
  "tags": [
    "developer",
    "project-management",
    "automation"
  ]
}
