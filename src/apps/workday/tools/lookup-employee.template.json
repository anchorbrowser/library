{
  "slug": "lookup-employee",
  "name": "Lookup Employee",
  "description": "Search for an employee in Workday by name or email.",
  "type": "tool",
  "file": "lookup-employee.ts",
  "app": "workday",
  "inputSchema": [
    { "display_name": "searchQuery", "type": "string", "required": true, "description": "Employee name or email to search" }
  ],
  "outputSchema": [
    { "display_name": "employeeId", "type": "string", "required": true, "description": "Employee ID" },
    { "display_name": "name", "type": "string", "required": true, "description": "Full name" },
    { "display_name": "email", "type": "string", "required": true, "description": "Email address" },
    { "display_name": "department", "type": "string", "required": false, "description": "Department" },
    { "display_name": "title", "type": "string", "required": false, "description": "Job title" }
  ]
}
