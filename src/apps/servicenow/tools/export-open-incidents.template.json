{
  "slug": "export-open-incidents",
  "name": "Export Open Incidents List",
  "description": "Export open incidents list from ServiceNow.",
  "type": "tool",
  "file": "export-open-incidents.ts",
  "app": "servicenow",
  "inputSchema": [
    {
      "display_name": "assignmentGroup",
      "type": "string",
      "required": false,
      "description": "Assignment group filter"
    },
    {
      "display_name": "priority",
      "type": "string",
      "required": false,
      "description": "Priority filter"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to incidents list"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether export completed"
    }
  ],
  "tags": [
    "itsm",
    "enterprise",
    "export"
  ]
}
