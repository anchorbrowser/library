{
  "slug": "add-candidate",
  "name": "Add Candidate",
  "description": "Add a new candidate to Lever ATS.",
  "type": "tool",
  "file": "add-candidate.ts",
  "app": "lever",
  "inputSchema": [
    { "displayName": "name", "type": "string", "required": true, "description": "Candidate full name" },
    { "displayName": "email", "type": "string", "required": true, "description": "Email address" },
    { "displayName": "posting", "type": "string", "required": true, "description": "Job posting to apply for" },
    { "displayName": "origin", "type": "string", "required": false, "description": "Candidate origin/source" }
  ],
  "outputSchema": [
    { "displayName": "candidateId", "type": "string", "required": true, "description": "Created candidate ID" },
    { "displayName": "success", "type": "boolean", "required": true, "description": "Whether candidate was added" }
  ]
}
