{
  "slug": "export-trip-invoices",
  "name": "Export Trip Invoices",
  "description": "Export trip invoices from TravelPerk.",
  "type": "tool",
  "file": "export-trip-invoices.ts",
  "app": "travelperk",
  "inputSchema": [
    {
      "display_name": "dateFrom",
      "type": "date",
      "required": true,
      "description": "Start date"
    },
    {
      "display_name": "dateTo",
      "type": "date",
      "required": true,
      "description": "End date"
    }
  ],
  "outputSchema": [
    {
      "display_name": "filePath",
      "type": "string",
      "required": true,
      "description": "Path to invoices"
    },
    {
      "display_name": "success",
      "type": "boolean",
      "required": true,
      "description": "Whether export completed"
    }
  ]
}
