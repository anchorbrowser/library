{
  "slug": "get-scheduled-events",
  "name": "Get Scheduled Events",
  "description": "Get scheduled events from Calendly.",
  "type": "tool",
  "file": "get-scheduled-events.ts",
  "app": "calendly",
  "inputSchema": [
    { "displayName": "dateRange", "type": "string", "required": false, "description": "Date range (today, week, month)", "defaultValue": "week" }
  ],
  "outputSchema": [
    { "displayName": "events", "type": "string", "required": true, "description": "JSON array of scheduled events" },
    { "displayName": "totalCount", "type": "number", "required": true, "description": "Total count" }
  ]
}
