{
  "slug": "create-bill",
  "name": "Create Bill",
  "description": "Create a new bill/expense in Xero.",
  "type": "tool",
  "file": "create-bill.ts",
  "app": "xero",
  "inputSchema": [
    { "display_name": "contactName", "type": "string", "required": true, "description": "Supplier/vendor name" },
    { "display_name": "description", "type": "string", "required": true, "description": "Bill description" },
    { "display_name": "amount", "type": "number", "required": true, "description": "Bill amount" },
    { "display_name": "dueDate", "type": "date", "required": false, "description": "Due date" },
    { "display_name": "accountCode", "type": "string", "required": false, "description": "Account code" }
  ],
  "outputSchema": [
    { "display_name": "billId", "type": "string", "required": true, "description": "Created bill ID" },
    { "display_name": "success", "type": "boolean", "required": true, "description": "Whether bill was created" }
  ]
}
