{
  "slug": "get-orders",
  "name": "Get Orders",
  "description": "Get orders from Faire.",
  "type": "tool",
  "file": "get-orders.ts",
  "app": "faire",
  "inputSchema": [
    { "display_name": "status", "type": "string", "required": false, "description": "Order status filter" },
    { "display_name": "limit", "type": "number", "required": false, "description": "Max orders to return" }
  ],
  "outputSchema": [
    { "display_name": "orders", "type": "string", "required": true, "description": "JSON array of orders" },
    { "display_name": "totalCount", "type": "number", "required": true, "description": "Total count" }
  ]
}
